[project]
name = "tw-stock-screener"
version = "0.1.0"
description = "Taiwan Stock Screener based on Master Bull Trend Template"
requires-python = ">=3.10"

[tool.pyright]
venvPath = "."
venv = ".venv"
extraPaths = [
    ".",
    ".venv/lib/python3.14/site-packages"
]
