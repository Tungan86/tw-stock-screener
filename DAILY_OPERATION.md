# 📊 台股量化選股監控系統 - 每日運作狀態與作業流程說明書

本說明文件詳細記錄系統在每日排程或手動執行時的**完整運作生命週期**、**各階段執行狀態**、**健康檢核標準**與**自動化維護建議**。

---

## 🔄 一、系統每日運作流程圖 (Pipeline Workflow)

```mermaid
flowchart TD
    A[🕒 盤後排程觸發 (建議每日 15:30)] --> B[階段 1: 環境與設定初始化]
    B --> C[階段 2: 同步台股上市櫃清單 (1,986 檔)]
    C --> D[階段 3: 多執行緒抓取歷史 K 線行情]
    D --> E[階段 4: 向量化計算技術指標 (均線/RSI/MACD/52W)]
    E --> F[階段 5: 執行大師多頭策略 8 大濾網]
    F --> G[階段 6: 多通道同步與歸檔]
    G --> G1[💻 終端機 Rich 彩色即時看板]
    G --> G2[📁 本地 CSV 報表產出]
    G --> G3[🌐 Google Sheets 自動寫入 Latest 與日期分頁]
    G --> G4[💾 本地 Parquet 歷史大數據壓縮封存]
```

---

## ⏱️ 二、建議執行時間與台股交易日考量

| 時段 | 市場狀態 | 系統建議動作 |
|---|---|---|
| **09:00 - 13:30** | 台股盤中交易 | 盤中即時行情變動大，不建議跑全市場完整 200MA/52W 結算。 |
| **13:30 - 14:30** | 盤後定價與零股交易 | 券商與 Yahoo Finance 伺服器正在進行盤後交易結算。 |
| **15:30 - 16:30 (最佳時間)** | **全日正式結算完畢** | **⭐️ 推薦每日執行時間**：證交所與各大數據源已完成當日收盤確認。 |
| **週末或國定假日** | 休市日 | 若於休市日執行，系統會抓取前一營業日最新收盤價，正常產生監控報表。 |

---

## 🔍 三、每日六大運作階段詳細說明

### 階段 1：環境與設定初始化
* **動作**：載入 `.env` 設定檔、Google 服務帳號金鑰（`service_account.json`）、確認日誌等級。
* **輸出標記**：
  ```text
  📅 執行基準日期: YYYY-MM-DD
  ```

### 階段 2：上市 / 上櫃清單維護
* **動作**：
  1. 檢查本機快取 `data/tw_stocks_registry.json`（預設有效期 7 天）。
  2. 若快取過期或加上 `--force-refresh`，則向台灣證交所 (TWSE) 與櫃買中心 (TPEx) 官方端點拉取最新清單。
  3. 自動過濾特別股、ETF、權證，純化出 1,986 檔普通股。
* **正常狀態日誌**：
  ```text
  ✅ 目標掃描標的數: 1986 檔
  ```

### 階段 3：歷史 K 線行情下載與快取 (OHLCV)
* **動作**：
  1. 檢查當日是否已有快取檔 `data/cache/market_ohlcv_YYYY-MM-DD.parquet`。
  2. 若當日已下載過，則**瞬間秒級載入快取**，不重複消耗網路頻寬。
  3. 若尚未下載，則啟動多執行緒（預設 8-10 線程）批次抓取過去 380 個交易日的日 K 線資料。
* **正常狀態日誌**：
  ```text
  ✅ 成功載入歷史行情: 1986 檔
  ```

### 階段 4 & 5：指標運算與「大師多頭」篩選
* **指標計算**：向量化計算 SMA 20/50/150/200、200MA 斜率、52 週最高/最低點、RSI(14)、MACD 柱狀體、ATR(14)、20MA 均量。
* **大師多頭策略 8 大過濾準則**：
  1. 價格高於 150MA 且高於 200MA
  2. 150MA 高於 200MA（長天期多頭排列）
  3. 200MA 維持上升斜率（大於 20 天前的值）
  4. 50MA 高於 150MA 且高於 200MA（中期均線多頭）
  5. 價格站上 50MA（短期攻擊結構）
  6. 距離 52 週低點反彈 $\ge 25\%$（脫離底部）
  7. 距離 52 週高點折價 $\le 25\%$（貼近波段新高）
  8. 20MA 均量 $\ge 500$ 張、價格 $\ge 10$ 元、RSI $\ge 50$
* **正常狀態日誌**：
  ```text
  INFO 大師多頭策略掃描完成: 檢驗 1986 檔，精選出 N 檔 (全數達標: N 檔)
  ```

### 階段 6：多通道結果同步與持久化
1. **終端機輸出**：透過 Rich Table 呈現彩色報表（漲紅跌綠、標示達成條件數）。
2. **本地 CSV 匯出**：自動輸出至 `output/master_bull_YYYY-MM-DD.csv`。
3. **Google Sheets 同步**：
   - 更新 **`Latest`** 分頁（最新清單覆蓋，表頭自動凍結加深色背景）。
   - 建立 **`YYYY-MM-DD`** 日期分頁（作為歷史選股軌跡）。
4. **本地 Parquet 歷史歸檔**：
   - 儲存至 `data/archive/screener_master_bull_YYYY-MM-DD.parquet`（高壓縮率、利於長期回測）。

---

## 📈 四、Google 試算表欄位對照說明

| 試算表欄位名稱 | 原程式欄位 | 意義與判讀技巧 |
|---|---|---|
| **股票代碼** | `code` | 台股 4 碼代號（例：2330） |
| **股票名稱** | `name` | 繁體中文簡稱（例：台積電） |
| **市場** | `market` | `TWSE`（上市）或 `TPEx`（上櫃） |
| **產業別** | `industry` | 官方產業類別（如：半導體業、光電業） |
| **收盤價** | `close` | 當日最新收盤成交價 |
| **漲跌幅(%)** | `pct_change` | 與前一交易日相較之百分比（正數多頭強勁） |
| **成交量(張)** | `volume_lots` | 當日總成交張數（千股） |
| **20日均量(張)**| `vol_ma20_lots` | 過去 20 天平均成交張數（流動性指標） |
| **量能倍數** | `vol_ratio` | 當日成交量 / 20日均量（$> 1.5$ 倍代表顯著帶量突破） |
| **50MA** | `sma_50` | 50 日均線（短期重要支撐線） |
| **150MA / 200MA** | `sma_150 / 200`| 中長天期生命線與年線 |
| **距52W高點(%)** | `pct_below_52w_high` | **核心指標**：數值越小代表越接近歷史新高（$< 10\%$ 為即將突破創新高） |
| **距52W低點(%)** | `pct_above_52w_low` | 股價自底部累積反彈幅度 |
| **RSI(14)** | `rsi_14` | 相對強弱指標（$50 \sim 70$ 為健康強勢多頭區） |
| **MACD柱狀** | `macd_hist` | 大於 0 代表多頭動能擴散 |
| **達成條件(8項)**| `passed_count` | 8/8 代表完美符合大師策略 |

---

## 🤖 五、自動化排程設定 (macOS / Linux Crontab)

如果您希望電腦在每天下午自動執行，不需手動輸入指令，可設定系統排程：

### 1. 開啟 Crontab 編輯器
```bash
crontab -e
```

### 2. 加入每日盤後排程 (週一至週五 下午 15:30)
```cron
30 15 * * 1-5 cd /Users/tongan/股市監控平台 && .venv/bin/python main.py >> output/cron.log 2>&1
```

### 3. 排程運作特點
* **自動靜默執行**：每日 15:30 一到，系統會自動在後台完成抓取、運算、寫入 Google Sheets。
* **日誌追蹤**：所有執行記錄會自動寫入 `output/cron.log`，方便隨時檢查。

---

## 🚨 六、常見狀態與異常排除指南

| 現象 / 錯誤訊息 | 可能原因 | 解決方式 |
|---|---|---|
| `快取完全命中 N 檔標的資料` | 當天已經跑過一次 | **正常現象**。系統會重複利用當日行情快取加速運算；若想強制重抓最新盤後修正數據，可加上 `--force-refresh`。 |
| `未發現完全符合「大師多頭策略」之標的` | 當日大盤重挫或處於空頭週期 | **正常現象**。大師策略對多頭趨勢要求嚴苛，若想看接近達標的候選股，可加上 `--all-candidates`。 |
| `Google Sheets 同步略過或失敗` | 服務帳號未加入試算表共用 | 請檢查該 Google Sheet 右上角「共用」，是否已將 `stock-screener@tw-stock-screener-507807.iam.gserviceaccount.com` 設為「編輯者」。 |
